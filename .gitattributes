# 텍스트 파일 줄바꿈(EOL)을 LF로 통일 — Windows(CRLF)/Mac/Linux(LF) 간 불필요한 diff 방지
* text=auto eol=lf

# Windows 배치 스크립트는 관례대로 CRLF 유지
*.bat text eol=crlf

# 리눅스/맥 셸 스크립트 — CRLF로 커밋되면 CI(GitHub Actions)나 맥/리눅스 환경에서 실행 자체가 깨질 수 있어 명시적으로 고정
gradlew text eol=lf